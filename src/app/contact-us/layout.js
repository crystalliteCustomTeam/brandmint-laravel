export const metadata = {
  title: "Contact Us - SEO Company",
  description: "At SEO Company, our clients are our number one priority and so we list our founders' contact information on our contact page. We always make ourselves",
}

export default function RootLayout({ children }) {
  return (children)
}
